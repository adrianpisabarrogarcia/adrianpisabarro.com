---
export interface Props {
  icon: string;
}
const { icon } = Astro.props as Props;
const svgPath = `../svg/${icon}.svg?raw`;
const { default: innerHTML } = await import(svgPath);
---

<Fragment set:html={innerHTML} />
